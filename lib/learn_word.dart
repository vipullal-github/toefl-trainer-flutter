class LearnWord {
  int id;
  int categoryId;
  String word;
  String meaning;
  String notes;

  LearnWord(
      {required this.id,
      required this.categoryId,
      required this.word,
      required this.meaning,
      required this.notes});
}
