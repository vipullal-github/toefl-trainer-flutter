class LearnWord {
  int id;
  String word;
  String meaning;
  String notes;

  LearnWord(
      {required this.id,
      required this.word,
      required this.meaning,
      required this.notes});
}
